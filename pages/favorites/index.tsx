import { Layout } from "@/components/layouts";

const Favorites = () => {
  return (
    <Layout title="Pokemons | Favorites">
      <div>Favorites</div>
    </Layout>
  );
};
export default Favorites;
